<% if resources.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(resources.errors.count, "error") %> erros:</h2>
    <ul>
      <% resources.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
